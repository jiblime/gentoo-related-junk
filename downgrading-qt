media-gfx/zbar# emerge -1av $(qlist -IC dev-qt)
# Rebuild the qt libraries for your preferred qt version. Mask unwanted versions appropriately.
# Notes: Do not mask: dev-qt/qtchooser or any package that does not follow the standard versioning in the dev-qt/ ca>

# emerge -1av $(qlist -IC kde-frameworks)
# Update version tags for anything KDE related before kde-plasma and kde-apps

# emerge the following: These use the Qt libraries and need to be rebuilt after chaning Qt versions.
# Example: 'emerge -1v app-crypt/qca media-gfx/zbar sys-auth/polkit-qt net-libs/accounts-qt dev-libs/libdbusmenu-qt >
#       It is _imperative_ that you use -1/--oneshot to prevent mucking up your world file

# emerge $(qlist -IC kde-plasma)
# Rebuild kde-plasma with correct Qt version tags

# emerge $(qlist -IC kde-apps)
# You get this

# Extranous notes: If simply rebuilding the KDE ecosystem, this works:     
#       'emerge -1avD $(qlist -IC kde-{frameworks,plasma,apps)'

Below is a set that you can add to sets to make it easier.
#     'emerge -1 @thisset'
app-crypt/qca
media-gfx/zbar
sys-auth/polkit-qt
net-libs/accounts-qt
dev-libs/libdbusmenu-qt
app-text/poppler
net-libs/signond
media-gfx/zbar# emerge -1av $(qlist -IC dev-qt)
# Rebuild the qt libraries for your preferred qt version. Mask unwanted versions appropriately.
# Notes: Do not mask: dev-qt/qtchooser or any package that does not follow the standard versioning in the dev-qt/ ca>

# emerge -1av $(qlist -IC kde-frameworks)
# Update version tags for anything KDE related before kde-plasma and kde-apps

# emerge the following: These use the Qt libraries and need to be rebuilt after chaning Qt versions.
# Example: 'emerge -1v app-crypt/qca media-gfx/zbar sys-auth/polkit-qt net-libs/accounts-qt dev-libs/libdbusmenu-qt >
#       It is _imperative_ that you use -1/--oneshot to prevent mucking up your world file

# emerge $(qlist -IC kde-plasma)
# Rebuild kde-plasma with correct Qt version tags

# emerge $(qlist -IC kde-apps)
# You get this

# Extranous notes: If simply rebuilding the KDE ecosystem, this works:     
#       'emerge -1avD $(qlist -IC kde-{frameworks,plasma,apps)'

app-crypt/qca
media-gfx/zbar
sys-auth/polkit-qt
net-libs/accounts-qt
dev-libs/libdbusmenu-qt
app-text/poppler
net-libs/signond
media-gfx/zbar
